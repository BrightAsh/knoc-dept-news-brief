# 분석 파이프라인

목표는 언론 보도 전체를 부서에 던지는 것이 아니라, 기사 안에서 한국석유공사와 각 부서가 실제로 봐야 할 문장 또는 문단만 모으는 것입니다.

## 단계

1. 기사 수집

   `scripts/collect.mjs`가 RSS와 sitemap에서 특정 날짜 기사를 모읍니다. `--include-body` 옵션을 켜면 기사 본문 `<p>` 문단도 함께 저장합니다.

2. 문단 후보 생성

   제목, 요약, 본문 문단을 분석 단위로 쪼갭니다. 제목은 신호가 강하므로 더 높은 가중치를 둡니다.

3. 규칙 기반 후보 압축

   22개 부서의 역할과 키워드로 1차 후보를 만듭니다. 이 단계는 비용이 없고 빠르기 때문에 LLM 입력량을 줄이는 필터 역할을 합니다.

4. LLM 문맥 검토

   LLM이 켜져 있으면 1차 후보 기사를 batch로 받아 다음을 판단합니다.

   - 한국석유공사 관점에서 볼 필요가 있는 기사인지
   - 어느 부서와 관련되는지
   - 근거가 되는 문장/문단은 무엇인지
   - 관련성 점수와 판단 이유는 무엇인지

5. 웹 UI 출력

   `briefs.json`에 `overall`과 `departments`를 저장합니다.

   - `overall`: 회사와 관련 있다고 판단된 기사 후보
   - `departments`: 부서별 근거 문장/문단

## 현재 산출물

```text
data/YYYY-MM-DD/briefs.json
```

핵심 필드:

- `overall.items[]`: 전체 회사 관련 기사 후보
- `departments[].items[]`: 부서별 근거
- `llm_review`: 사용한 provider, model, 처리 건수, 오류 상태

## 운영 원칙

- LLM 호출 실패 시에도 규칙 기반 결과를 남깁니다.
- 모델명과 provider는 환경변수로 교체합니다.
- 본문 전체를 무조건 LLM에 넣지 않고, 제목·요약·본문 일부만 사용합니다.
- 요약 생성은 다음 단계로 분리합니다. 먼저 “관련 기사와 근거를 정확히 고르는 것”을 우선합니다.
